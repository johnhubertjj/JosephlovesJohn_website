name: quality-checks

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  pre-commit:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Set up uv
        uses: astral-sh/setup-uv@v5

      - name: Sync dev dependencies
        run: uv sync --frozen --group dev

      - name: Run pre-commit (changed files)
        env:
          SKIP: no-commit-to-branch
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            uv run pre-commit run \
              --from-ref "origin/${{ github.base_ref }}" \
              --to-ref HEAD \
              --show-diff-on-failure \
              --color=always
          else
            if git rev-parse --verify HEAD~1 >/dev/null 2>&1; then
              uv run pre-commit run \
                --from-ref HEAD~1 \
                --to-ref HEAD \
                --show-diff-on-failure \
                --color=always
            else
              uv run pre-commit run --all-files --show-diff-on-failure --color=always
            fi
          fi

  ruff:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Set up uv
        uses: astral-sh/setup-uv@v5

      - name: Sync dev dependencies
        run: uv sync --frozen --group dev

      - name: Run Ruff
        run: uv run ruff check josephlovesjohn_site main_site mastering tests

  mypy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Set up uv
        uses: astral-sh/setup-uv@v5

      - name: Sync dev dependencies
        run: uv sync --frozen --group dev

      - name: Run Mypy
        env:
          DJANGO_SETTINGS_MODULE: josephlovesjohn_site.settings
        run: uv run mypy

  pytest:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Set up uv
        uses: astral-sh/setup-uv@v5

      - name: Sync dev dependencies
        run: uv sync --frozen --group dev

      - name: Run Pytest
        env:
          DJANGO_SETTINGS_MODULE: josephlovesjohn_site.settings
        run: uv run pytest -q

  coverage:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Set up uv
        uses: astral-sh/setup-uv@v5

      - name: Sync dev dependencies
        run: uv sync --frozen --group dev

      - name: Run Pytest with coverage threshold
        env:
          DJANGO_SETTINGS_MODULE: josephlovesjohn_site.settings
        run: uv run pytest --cov --cov-report=term-missing -q
